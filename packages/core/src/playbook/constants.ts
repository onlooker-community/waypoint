/** Lazy dedup runs when a task type has more than this many bullets. */
export const PLAYBOOK_DEDUP_THRESHOLD = 50;

/** Max bullets injected into hint generation. */
export const PLAYBOOK_RETRIEVAL_TOP_K = 5;
